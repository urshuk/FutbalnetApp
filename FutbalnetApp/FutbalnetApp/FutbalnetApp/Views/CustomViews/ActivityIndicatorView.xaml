<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="ActInd"
             x:Class="FutbalnetApp.Views.ActivityIndicatorView">
    <ContentView.Content>
        <AbsoluteLayout>
            <ContentView BackgroundColor="Black"
                         Opacity="0.5"
                         AbsoluteLayout.LayoutFlags="All"
                         AbsoluteLayout.LayoutBounds="0,0,1,1"
                         IsVisible="{Binding IsRunning, Source={Reference ActInd}}"/>
            <ActivityIndicator AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"
                               AbsoluteLayout.LayoutFlags="PositionProportional"
                               IsRunning="{Binding IsRunning, Source={Reference ActInd}}"
                               IsVisible="{Binding IsRunning, Source={Reference ActInd}}"/>
        </AbsoluteLayout>
    </ContentView.Content>
</ContentView>