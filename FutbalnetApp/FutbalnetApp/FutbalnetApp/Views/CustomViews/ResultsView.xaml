<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Results"
             x:Class="FutbalnetApp.Views.ResultsView">
    <ContentView.Resources>
        <DataTemplate x:Key="ResultItem">
            <Grid HeightRequest="100" BackgroundColor="{DynamicResource AlternativeColor}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Image Grid.Column="0" Grid.Row="0" Aspect="AspectFit" Source="{Binding Teams[0].Club.Logo}"/>
                <Label Grid.Column="0" Grid.Row="1" FontFamily="{StaticResource NormalFont}" FontSize="12" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Text="{Binding Teams[0].Name}"/>
                <Label Grid.Column="1" Grid.Row="0" FontFamily="{StaticResource NormalFont}" FontSize="20" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Text="{Binding ScoreString}" TextColor="{DynamicResource HighlightColor}"/>
                <Label Grid.Column="1" Grid.Row="1" FontFamily="{StaticResource NormalFont}" FontSize="10" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Text="{Binding Datetime, StringFormat='{0:dd.MM.yyyy hh:mm}'}"/>
                <Image Grid.Column="2" Grid.Row="0" Aspect="AspectFit" Source="{Binding Teams[1].Club.Logo}"/>
                <Label Grid.Column="2" Grid.Row="1" Padding="0" LineBreakMode="WordWrap" FontFamily="{StaticResource NormalFont}" FontSize="12" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Text="{Binding Teams[1].Name}"/>
            </Grid>
        </DataTemplate>
        <Style x:Key="ResultsList" TargetType="CollectionView">
            <Setter Property="ItemTemplate" Value="{StaticResource ResultItem}"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="VerticalOptions" Value="FillAndExpand"/>
            <Setter Property="ItemsLayout">
                <Setter.Value>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentView.Resources>
  <ContentView.Content>
        <StackLayout Margin="0,25,0,0" Spacing="10">
            <StackLayout Orientation="Horizontal">
                <Label TextColor="{DynamicResource TextColor}" Text="Kolo" VerticalTextAlignment="Center" HorizontalOptions="Start"/>
                <Picker BackgroundColor="{DynamicResource AlternativeColor}" TextColor="{DynamicResource TextColor}" VerticalOptions="Center" ItemsSource="{Binding Part.Rounds, Source={Reference Results}}" SelectedItem="{Binding Round, Source={Reference Results}, Mode=TwoWay}" Unfocused="OnRoundChanged"/>
                <Label TextColor="{DynamicResource TextColor}" Text="{Binding Round.Datetime, Source={Reference Results}, StringFormat='{0:dd.MM.yyyy}'}" VerticalTextAlignment="Center" HorizontalTextAlignment="End" HorizontalOptions="EndAndExpand"/>
            </StackLayout>
            <CollectionView ItemsSource="{Binding ItemsSource, Source={Reference Results}}"
                        Style="{StaticResource ResultsList}"
                        SelectionChanged="OnSelectionChanged"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>