<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Nastavenia"
             x:Class="FutbalnetApp.Views.SettingsPage">
    <ContentPage.Resources>
        <Style TargetType="Label" x:Key="BaseLabel">
            <Setter Property="FontFamily" Value="{StaticResource NormalFont}" />
            <Setter Property="TextColor" Value="{DynamicResource TextColor}" />
            <Setter Property="HorizontalOptions" Value="StartAndExpand" />
            <Setter Property="HeightRequest" Value="30" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="VerticalTextAlignment" Value="Center" />
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <TableView Intent="Menu" HasUnevenRows="True" BackgroundColor="{DynamicResource BackgroundColor}">
            <TableRoot>
                <TableSection Title="Tipy">
                    <ViewCell>
                        <StackLayout Padding="15,0,15,5" Spacing="0">
                            <Label Text="Obľúbené" Style="{StaticResource BaseLabel}"/>
                            <Label Text="Pridaj si ligy, kluby a tímy do zoznamu obľúbených a urýchliš si tým cestu k výsledkom" VerticalTextAlignment="Start" HeightRequest="-1" TextColor="{DynamicResource DarkAlternativeColor}" Style="{StaticResource BaseLabel}"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell>
                        <StackLayout Padding="15,0,15,5" Spacing="0">
                            <Label Text="Časová os" Style="{StaticResource BaseLabel}"/>
                            <Label Text="Na časovej osi sa ti zobrazia najbližšie zápasy tvojich obľúbených tímov" VerticalTextAlignment="Start" HeightRequest="-1" TextColor="{DynamicResource DarkAlternativeColor}" Style="{StaticResource BaseLabel}"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
                <TableSection Title="Napíš nám">
                    <ViewCell>
                        <StackLayout Padding="15,0">
                            <Label Text="Nahlás problém" Padding="0, 10" Style="{StaticResource BaseLabel}"/>
                            <Editor x:Name="BugEditor" Keyboard="Text" AutoSize="Disabled" HeightRequest="75" TextColor="{DynamicResource TextColor}" BackgroundColor="{DynamicResource BackgroundColor}" PlaceholderColor="{DynamicResource DarkAlternativeColor}" Placeholder="Opíš čo najpodrobnejšie problém..."/>
                            <Button Text="Odoslať" HorizontalOptions="Center" Clicked="BugSendClicked" FontSize="15" TextColor="{DynamicResource HighlightColor}"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell>
                        <StackLayout Padding="15,0">
                            <Label Text="Más nápad?" Padding="0, 10" Style="{StaticResource BaseLabel}"/>
                            <Editor x:Name="FeedbackEditor" Keyboard="Text" AutoSize="Disabled" HeightRequest="75" TextColor="{DynamicResource TextColor}" BackgroundColor="{DynamicResource BackgroundColor}" PlaceholderColor="{DynamicResource DarkAlternativeColor}" Placeholder="Daj nám vedieť čo by si zlepšil a ako..."/>
                            <Button Text="Odoslať" HorizontalOptions="Center" Clicked="FeedbackSendClicked" FontSize="15" TextColor="{DynamicResource HighlightColor}"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
                <TableSection Title="O aplikácii">
                    <ViewCell Height="30">
                        <StackLayout Orientation="Horizontal" Padding="15,0">
                            <Label Text="Autor" Style="{StaticResource BaseLabel}"/>
                            <Label Text="Vít Vašák" Style="{StaticResource BaseLabel}" TextColor="{DynamicResource DarkAlternativeColor}" HorizontalOptions="End" HorizontalTextAlignment="End"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell Height="30">
                        <StackLayout Orientation="Horizontal" Padding="15,0">
                            <Label Text="Dizajnér" Style="{StaticResource BaseLabel}"/>
                            <Label Text="Libi Bujňáková" Style="{StaticResource BaseLabel}" TextColor="{DynamicResource DarkAlternativeColor}" HorizontalOptions="End" HorizontalTextAlignment="End"/>
                        </StackLayout>
                    </ViewCell>
                    <ViewCell Height="30">
                        <StackLayout Orientation="Horizontal" Padding="15,0">
                            <Label Text="Verzia" Style="{StaticResource BaseLabel}"/>
                            <Label Text="1.0b" Style="{StaticResource BaseLabel}" TextColor="{DynamicResource DarkAlternativeColor}" HorizontalOptions="End" HorizontalTextAlignment="End"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
            </TableRoot>
        </TableView>
    </ContentPage.Content>
</ContentPage>